InsightHub accepts one file per upload operation. The starter limit is 10 MiB per file. Text extraction is limited to 200,000 characters, and a PDF may contain at most 100 pages.

Upload operations have a 120-second deadline. Chat operations have a 60-second deadline. A repeated idempotency key with a different payload must return a conflict.
